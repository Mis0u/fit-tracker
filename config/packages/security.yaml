security:
    # https://symfony.com/doc/current/security.html#registering-the-user-hashing-passwords
    password_hashers:
        Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface: 'auto'

    # https://symfony.com/doc/current/security.html#loading-the-user-the-user-provider
    providers:
        # used to reload user from session & other features (e.g. switch_user)
        app_user_provider:
            entity:
                class: App\Entity\User
                property: email

    firewalls:
        dev:
            # Ensure dev tools and static assets are always allowed
            pattern: ^/(_profiler|_wdt|assets|build)/
            security: false
        main:
            lazy: true
            provider: app_user_provider
            user_checker: App\Security\BlockedUserChecker
            form_login:
                login_path: app_login
                check_path: app_login
                enable_csrf: true
            logout:
                path: app_logout
                target: app_login
            remember_me:
                secret: '%kernel.secret%'
                lifetime: 2592000 # 1 mois
                always_remember_me: false
            login_throttling:
                max_attempts: 3 # PAR MINUTE

            # Activate different ways to authenticate:
            # https://symfony.com/doc/current/security.html#the-firewall

            # https://symfony.com/doc/current/security/impersonating_user.html
            # switch_user: true

    # Note: Only the *first* matching rule is applied
    access_control:
        - { path: ^/admin, roles: ROLE_ADMIN }
        # LiveComponents (Symfony UX) ne passent pas par un Single Action Controller — pas de
        # #[IsGranted] classe possible. Sans cette règle, une session expirée en pleine séance
        # laisse passer l'appel AJAX du composant anonyme, et App\Twig\Components\LiveComponent\
        # ExerciseSelectorComponent::getUser() plante (Security::getUser() retourne null alors que
        # le type de retour est User, non-nullable) au lieu d'un rejet propre.
        - { path: '^/[a-z]{2}/_components', roles: ROLE_USER }
        # - { path: ^/profile, roles: ROLE_USER }

when@test:
    security:
        password_hashers:
            # Password hashers are resource-intensive by design to ensure security.
            # In tests, it's safe to reduce their cost to improve performance.
            Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface:
                algorithm: auto
                cost: 4 # Lowest possible value for bcrypt
                time_cost: 3 # Lowest possible value for argon
                memory_cost: 10 # Lowest possible value for argon
        firewalls:
            main:
                # La suite E2E Playwright (tests/e2e/) fait de vraies connexions HTTP (pas de
                # transactions DAMA en rollback comme PHPUnit) : plusieurs specs se logguent
                # réellement dans la même fenêtre d'une minute et dépassent vite la limite prod de
                # 3/minute, faisant échouer des tests sans rapport avec le throttling lui-même.
                login_throttling:
                    max_attempts: 30 # PAR MINUTE — prod reste à 3, cf. bloc racine ci-dessus
